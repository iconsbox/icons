{
  "name": "iconbox",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "lerna run --parallel generate && node ./scripts/icons/docsBuild.js",
    "change-version": "lerna run --parallel change-version",
    "build": "lerna run --parallel build",
    "publish-npm": "lerna run --parallel publish-npm",
    "optimize": "lerna run --parallel optimize",
    "test": "echo \"Will write tests soon ....\""
  },
  "devDependencies": {
    "esbuild": "^0.14.43",
    "fs-extra": "^10.1.0",
    "glob": "^8.0.3",
    "lerna": "^5.0.0",
    "node-html-parser": "^5.3.3",
    "prettier": "^2.6.2",
    "rimraf": "^3.0.2",
    "semver": "^7.3.7",
    "shelljs": "^0.8.5"
  }
}
